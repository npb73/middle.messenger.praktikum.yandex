import * as style from './style.module.sass'

export const tmpl = 
`
<div class="${style.inputWrapper}">
  <input type="button" class="${style.customAvatar}" value="set avatar" name="avatar"/>
</div>

`
